import focus from '../index.js'

describe('constructor', function () {
  it('cteates new instance', function () {
    const instance = new focus()
    expect(instance).toBeInstanceOf(focus)
  })

  // TODO:
})
